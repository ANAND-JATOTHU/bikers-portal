# ================================================
# BIKERS PORTAL - ENVIRONMENT CONFIGURATION
# ================================================
# This is a template file. Copy this to .env and fill in your values.
# Command: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)

# ================================================
# APPLICATION SETTINGS
# ================================================

# Environment Mode
# Options: development | production | test
# - development: Shows detailed errors, uses nodemon auto-restart
# - production: Optimized for performance, minimal error details
NODE_ENV=development

# Server Port
# The port where your application will run
# Default: 3000 (you can use any available port like 5000, 8080, etc.)
PORT=3000


# ================================================
# DATABASE CONFIGURATION - MONGODB
# ================================================
# Choose ONE of the options below and uncomment it:

# --------------------------------------------------
# OPTION 1: Local MongoDB (For Local Development)
# --------------------------------------------------
# Prerequisites: Install MongoDB Community Server on your machine
# Download: https://www.mongodb.com/try/download/community
# After installation, MongoDB runs on localhost:27017 by default
# Database name: bikers-portal (will be created automatically)

MONGODB_URI=mongodb://localhost:27017/bikers-portal

# --------------------------------------------------
# OPTION 2: MongoDB Atlas (Cloud Database - Recommended)
# --------------------------------------------------
# Steps to get your connection string:
# 1. Go to https://www.mongodb.com/cloud/atlas
# 2. Create a free account (no credit card required)
# 3. Create a new cluster (M0 Sandbox - FREE tier, 512MB)
# 4. Create a database user (Database Access section)
# 5. Add your IP to whitelist (Network Access section)
#    Tip: Use 0.0.0.0/0 to allow from anywhere (for development only!)
# 6. Click "Connect" -> "Connect your application" -> Copy connection string
# 7. Replace <password> with your database user password
# 8. Replace <dbname> with "bikers-portal" (or your preferred name)
#
# Example format:
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/bikers-portal?retryWrites=true&w=majority
#
# Uncomment and paste your connection string here:
# MONGODB_URI=

# --------------------------------------------------
# OPTION 3: MongoDB with Authentication (Advanced)
# --------------------------------------------------
# For local MongoDB with authentication enabled:
# MONGODB_URI=mongodb://username:password@localhost:27017/bikers-portal?authSource=admin


# ================================================
# SESSION CONFIGURATION
# ================================================

# Session Secret Key
# CRITICAL SECURITY SETTING: This secret is used to sign session cookies
# 
# For Development:
# - You can use the default value below for local testing
# 
# For Production:
# - MUST generate a strong, random secret key
# - Never reuse the same secret across different environments
# - Keep this value absolutely secret!
#
# Generate a secure random secret (run this command in terminal):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Or use this online tool: https://www.grc.com/passwords.htm

SESSION_SECRET=your-super-secret-session-key-change-in-production

# Session expiry time (optional - already set in code)
# Uncomment to override default (default: 7 days)
# SESSION_MAX_AGE=604800000


# ================================================
# FILE UPLOAD CONFIGURATION
# ================================================

# Maximum file size for uploads (in bytes)
# Current: 5MB (5 * 1024 * 1024 = 5242880 bytes)
# Common sizes:
# - 1MB  = 1048576
# - 5MB  = 5242880
# - 10MB = 10485760
# - 50MB = 52428800
MAX_FILE_SIZE=5242880

# Upload directory path
# Where uploaded files (bike images, profile pictures) will be stored
# Relative to project root
UPLOAD_PATH=./public/uploads


# ================================================
# OPTIONAL CONFIGURATIONS
# ================================================

# Email Configuration (for future email features)
# Uncomment and configure when you add email functionality:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password
# EMAIL_FROM=noreply@bikersportal.com

# JWT Configuration (if you switch to JWT authentication instead of sessions)
# JWT_SECRET=your-jwt-secret-key
# JWT_EXPIRE=7d

# Cloudinary Configuration (for cloud image storage instead of local uploads)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# Application URL (useful for email links, webhooks, etc.)
# APP_URL=http://localhost:3000

# Logging Level
# Options: error | warn | info | debug
# LOG_LEVEL=info


# ================================================
# QUICK START GUIDE
# ================================================
#
# STEP 1: Install MongoDB
# ├─ Option A: Install MongoDB locally
# │  └─ Download from: https://www.mongodb.com/try/download/community
# │
# └─ Option B: Use MongoDB Atlas (cloud)
#    └─ Sign up at: https://www.mongodb.com/cloud/atlas
#
# STEP 2: Configure MONGODB_URI above
# ├─ For Local: mongodb://localhost:27017/bikers-portal
# └─ For Atlas: Paste your connection string
#
# STEP 3: Generate a strong SESSION_SECRET for production
# └─ Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# STEP 4: Save this file as .env and restart your server
# └─ Run: npm start (or npm run dev for development)
#
# STEP 5: Access the application
# └─ Open: http://localhost:3000
#
# ================================================

# Need help? Check the README.md file or raise an issue on GitHub!
